[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @fallen-sparrow/shared build",
  "pnpm --filter @fallen-sparrow/server build"
]

[start]
cmd = "pnpm --filter @fallen-sparrow/server start"
